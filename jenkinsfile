 pipeline {
        agent none
        tools {
          maven 'maven3.9.3'
        }
        stages {
          stage("build & SonarQube analysis") {
            agent any
            steps {
              withSonarQubeEnv('SonarQube') {
                sh 'mvn clean verify sonar:sonar -Dsonar.host.url=http://localhost:9000  -Dsonar.projectKey=SonarQubeTest2 -Dsonar.login=sqa_f4b0561f0081792a4b7ab6a134f0e1ec120275cb'
              }
            }
          }
          stage("Quality Gate") {
            steps {
              timeout(time: 1, unit: 'HOURS') {
                waitForQualityGate abortPipeline: true
              }
            }
          }
        }
      }
