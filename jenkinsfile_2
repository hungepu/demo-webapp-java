 pipeline {
        agent any
        tools {
          jdk 'JDK11'
          maven 'maven-3.6.1'
        }
        stages {
          stage("SonarQube analysis") {
            steps {
              withSonarQubeEnv('SonarQube') {
                sh 'sonar:sonar -Dsonar.projectKey=SonarQubeTest2'
              }
            }
          }
          stage("Quality Gate") {
            steps {
              timeout(time: 1, unit: 'HOURS') {
                waitForQualityGate abortPipeline: true
              }
            }
          }
          stage("build") {
            agent any
            steps {
              withSonarQubeEnv('SonarQube') {
                sh 'mvn clean install'
              }
            }
          }
        }
      }
